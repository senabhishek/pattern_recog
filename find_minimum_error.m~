function [min_error, featsize_at_min_error] = find_minimum_error(error_matrix)
  min_error = 0;
  featsize_at_min_error = 0;
  err_matrix_size = length(error_matrix);
  temp_min_error = 1;
  temp_min_featsize = 23;
  for i=1:err_matrix_size
    
  end
end

